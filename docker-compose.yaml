services:
  blink2mqtt:
    image: graystorm/blink2mqtt:latest
    network_mode: host
    container_name: blink2mqtt
    restart: unless-stopped
    volumes:
      - /volume1/docker/blink2mqtt/config:/config

